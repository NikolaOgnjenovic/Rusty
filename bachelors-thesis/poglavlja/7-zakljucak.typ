= Закључак
<zakljucak>

У овом раду анализирана је и документована имплементација пројекта `Rust-ECS`,
са посебним фокусом на избор ECS архитектуре, њену практичну примену у
`rusty` библиотеци и везу са два демонстрациона пројекта:
`text-game` и `rendering-benchmark`.

Кроз структуру рада показано је да се кључне архитектонске одлуке могу пратити
од теоријске мотивације, преко имплементационих детаља, до емпиријске провере у
контролисаном сценарију. Тај континуитет је био важан методолошки циљ, јер
омогућава да закључци не остану на нивоу општих тврдњи, већ да буду ослоњени на
конкретне ефекте у коду и у понашању система.

Показано је да ECS приступ доноси јасно раздвајање података и логике,
једноставније проширење функционалности и добру основу за тестирање. У конкретној
имплементацији то се огледа у централној структури `World`, типски раздвојеним
складиштима компоненти и систему догађаја који смањује спрегу између делова
програма.

Посебан допринос рада је и аргументовано образложење избора Rust-а као
имплементационог језика. Пројекат је послужио као практичан тест да ли је могуће
изградити употребљив ECS и рендеринг подсистем без увођења `unsafe` кода у
апликативном слоју. У датом обиму и циљевима рада, показано је да је то могуће,
уз прихватљив однос између сложености развоја и добијене поузданости.

У поређењу са `C` приступом, Rust је у овом пројекту смањио простор за класичне
memory-safety грешке и померио фокус на дисциплинованији дизајн интерфејса. У
поређењу са `C#` приступом, цена је нешто већи иницијални напор у моделовању
власништва и приступа подацима, али уз већу контролу над ресурсима и
предвидивије понашање у сценаријима који су ближи системском програмирању.
Практично, то значи да је „цимање“ израженије на почетку, али да каснији развој
добија на стабилности.

Рендеринг подсистем реализован је преко `wgpu` библиотеке, што обезбеђује
преносивост и модеран GPU модел уз прихватљиву сложеност. Истовремено, бенчмарк
сценарио показује како се архитектонске одлуке одражавају на перформансе у
условима променљивог оптерећења.

Евалуациони део је показао да су трендови перформанси довољно информативни за
дијагностику уских грла и приоритизацију даљих оптимизација. Иако бенчмарк није
замена за дубинско профилисање, његова вредност је у томе што даје брз,
репродуктиван увид у везу између архитектуре и оперативног понашања.

Главна ограничења тренутног решења су `HashMap` организација компоненти и
секвенцијално извршавање система, што представља простор за даља унапређења.
Као природан наставак развоја издвајају се аркетајп складиште, паралелизација,
као и проширење бенчмарк сценарија и комплетнија анализа метрика.

У ширем смислу, резултат рада показује да је за академски и прототипски контекст
оправдано кренути од архитектонски чистог и безбедног решења, па тек затим
уводити агресивније оптимизације где мерења покажу да су неопходне. Такав редослед
смањује ризик од преране сложености и омогућава да свако унапређење буде
аргументовано мерљивим ефектом.
