= Архитектура пројекта `Rust-ECS`

Архитектура пројекта постављена је тако да раздвоји језгро погона од
апликативних сценарија који то језгро користе. На највишем нивоу, решење је
подељено на три целине:

- `rusty` — библиотека са ECS моделом, догађајима и рендеринг
  подсистемом,
- `text-game` — конзолна апликација која показује ECS ток без графичке
  зависности,
- `rendering-benchmark` — графичка апликација за емпиријско испитивање
  перформанси.

Оваква подела је важна јер омогућава независну еволуцију доменске логике,
рендеринга и тест сценарија. Истовремено, одржава се јединствен API ослонац у
библиотеци `rusty`.

== Модуларна организација

Библиотека `rusty` представља централни слој система. Она дефинише:

- апстракције за ентитете и компоненте,
- менаџмент животног циклуса ентитета,
- регистрацију и приступ компонентама,
- догађајни механизам за комуникацију између система,
- рендеринг модул који користи ECS податке као улаз.

Кључна инжењерска одлука јесте да се апликациони код (`text-game`,
`rendering-benchmark`) ослања на јавни интерфејс библиотеке, без директног
мењања њених интерних структура. На тај начин се постиже чиста граница између
платформе и конкретне примене.

== Архитектонски слојеви

Посматрано по слојевима, пројекат може да се представи као:

- #emph[слој података] — компоненте које описују стање,
- #emph[слој логике] — системи који обрађују скупове компоненти и догађаја,
- #emph[слој интеграције] — апликације које покрећу петљу ажурирања,
- #emph[слој приказа] — рендеринг подсистем који преводи ECS стање у GPU позиве.

Ова слојевита организација је практична из најмање три разлога. Прво, омогућава
локализацију промена: измене у систему борбе не захтевају измене у рендереру.
Друго, поједностављује тестирање јер се делови могу посматрати изоловано. Треће,
олакшава проширење новим функционалностима, нпр. додавањем аудио подсистема без
промене постојећег ECS језгра.

Из угла Rust језика, овакво слојевито раздвајање има и додатну техничку вредност.
Јасне границе између слојева природно смањују потребу за сложеним шемама
позајмљивања јер сваки слој има прецизније дефинисан скуп улаза и излаза. То је
посебно важно у ECS окружењу где више система приступа заједничком стању. Уместо
да се ослања на имплицитна правила дисциплине кода, Rust компајлер форсира да
границе зависности буду експлицитне, што у пракси доводи до чистијег API дизајна.

== Ток података и контроле

У типичном циклусу извршавања ток је следећи:

1. апликација креира или ажурира ентитете,
2. системи читају/мењају компоненте и обрађују догађаје,
3. рендеринг слој прикупља видљиве ентитете,
4. подаци се пакују у GPU-оријентисан формат,
5. кадар се исцртава и циклус се понавља.

Овим се раздваја #emph[симулација света] од #emph[визуелизације света]. То је
кључно за стабилан дизајн јер логичка исправност игре не зависи директно од
графичког backend-а.

== Архитектонска проверљивост и одрживост кода

Једна од практичних предности изабране архитектуре је што омогућава лакшу
проверљивост кода током развоја. Када су границе између модула јасне, промене у
једном делу система лакше се локализују и евалуирају:

- измене у догађајном моделу могу се проверити без директног утицаја на рендерер,
- измене у рендеринг слоју могу се мерити бенчмарком без промене доменске логике,
- измене у `World` API-ју могу се валидирати кроз оба демонстрациона пројекта.

Оваква проверљивост је усклађена са академским циљем рада: да архитектонске
тврдње буду поткрепљене конкретним, поновљивим корацима валидације.

Поред тога, одрживост кода је унапређена тиме што су интерфејси усклађени са
Rust типским ограничењима. Иако то у неким случајевима захтева више иницијалног
дизајна, резултат је јаснија структура зависности и мањи ризик да се временом
појаве „скривене пречице“ које компликују одржавање.

== Одлуке о дизајну и компромиси

Иако је циљ био да се добије архитектура погодна и за анализу и за практичну
употребу, донети су одређени компромиси:

- приоритет је дат читљивости и јасном API-ју у односу на максималну микро-
  оптимизацију,
- складиште компоненти је засновано на `HashMap` организацији ради
  једноставности,
- систем извршавања је секвенцијалан, што поједностављује детерминизам, али
  ограничава скалирање на више језгара.

Ови компромиси су свесни и одговарају циљу рада: изградити стабилну,
разумљиву основу која омогућава даља унапређења (архетајп складиште,
паралелизација система, проширени scheduler).

У овом делу је важно нагласити и један практичан аспект избора Rust-а у односу на
`C` и `C#`. У `C`-у би слична архитектура могла да се имплементира са мање
апстракционих ограничења компајлера, али би верификација исправности управљања
меморијом и животним веком објеката остала у већој мери ручна. У `C#`-у је део те
сложености делегиран runtime-у, али тиме расте зависност од GC понашања и
динамичке алокације у сценаријима интензивног ажурирања стања. Rust захтева више
архитектонске дисциплине у старту, али заузврат даје детерминисанији модел рада
са ресурсима, што је у контексту играчког погона значајна предност.

Практично гледано, „цимање“ је највеће у раној фази када се дефинишу ownership
границе између `World`-а, система и рендерера. Када се једном усвоји pattern да
системи приступају подацима преко уских и добро типизованих интерфејса, темпо
развоја постаје стабилнији, а број регресија мањи. То је и главни разлог зашто је
Rust оцењен као оправдан избор за овај рад.

Овај закључак је важан и из перспективе будућег тима или наставка пројекта.
Када се нови функционални захтеви уводе на постојећу базу, јасне ownership
границе делују као архитектонска „ограда“ која смањује број неочекиваних спојних
тачака између модула. На тај начин се и код сложенијих проширења задржава
предвидивост система.

== Однос библиотеке и демонстрационих пројеката

Апликација `text-game` је минималан, али методолошки значајан пример јер
доказује да ECS архитектура није везана искључиво за графику. Системи обрађују
догађаје и мењају стање ентитета без икаквог рендеринг слоја.

С друге стране, `rendering-benchmark` представља стрес-тест графичког пута:
масовно ажурирање компоненти и рендеровање великог броја објеката у реалном
времену. Комбинацијом ова два примера добија се потпунија слика о
употребљивости језгра у различитим контекстима.

Ова двострука валидација је методолошки значајна и за процену самог језика.
`text-game` показује како Rust и ECS сарађују у чисто симулационом сценарију, док
`rendering-benchmark` тестира да ли исти архитектонски темељ остаје употребљив и
под графичким оптерећењем. На тај начин се избегава једнострано закључивање на
основу само једне врсте апликације.

== Позиционирање у односу на савремене погоне

Савремени комерцијални и open-source погони често користе варијанте ECS-а или
хибридне моделе. Пројекат `Rust-ECS` не тежи директној конкуренцији таквим
системима, већ образовној и истраживачкој вредности. Ипак, применом истих
основних принципа (композиција, data-oriented дизајн, одвајање симулације и
рендера) поставља се темељ који је компатибилан са индустријском праксом.

Управо та компатибилност концепата је важна: студентски пројекат остаје
довољно мали за детаљну анализу, али архитектонски довољно зрел да се из њега
изводе релевантни закључци о дизајну играчких система.

