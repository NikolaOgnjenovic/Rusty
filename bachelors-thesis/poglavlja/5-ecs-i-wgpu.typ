= Рендеринг подсистем и избор `wgpu`

Рендеринг подсистем у оквиру `rusty` представља мост између ECS модела
света и графичког уређаја. Његова примарна улога није само исцртавање објеката,
већ и контролисано пресликавање компонентних података у формат погодан за GPU
обраду.

== Улога рендеринга у укупној архитектури

У ECS архитектури рендеринг је посматран као потрошач података из света. Он не
дефинише правила симулације, већ визуализује њен резултат. Ова дистинкција је
важна јер:

- логичка коректност игре остаје независна од графичког API-ја,
- исти симулациони модел може да ради и без графичког излаза,
- рендеринг се може мењати или унапређивати без нарушавања доменских система.

У практичном коду, рендеринг слој из `World`-а прикупља компоненте релевантне за
приказ (позиција, спрајт, видљивост, редослед исцртавања), формира GPU инстанце и
покреће цртање кадра.

== `Renderer2D` као кључна апстракција

Структура `Renderer2D` обједињује иницијализацију графичког контекста и сам
процес рендеровања. Њена одговорност обухвата:

- креирање `wgpu` инстанце и избор адаптера,
- добијање уређаја и командног реда,
- конфигурисање површине за приказ,
- припрему pipeline-а, бафера и bind група,
- извршавање draw позива у оквиру рендеринг пролаза.

#figure(
```rust
pub fn render_world(&mut self, world: &World) -> Result<(), RenderError> {
    let batch = collect_visible_sprites(world);
    if batch.is_empty() {
        return self.render_batch(&[]);
    }
    let mut gpu_instances = Vec::with_capacity(batch.len());
    // мапирање ECS компоненти у GPU инстанце
    self.render_batch_with_order(&gpu_instances, &batch)
}
```,
  caption: [Прелаз из ECS стања у GPU инстанце]
) <lst-renderer-world-path>

Овај ток потврђује да је рендеринг у систему моделован као чиста трансформација
подаци -> визуелна репрезентација.

Ова архитектонска одлука је значајна и за анализу избора Rust-а. Када се
рендеринг третира као трансформациони слој, лакше је одржати строге ownership
границе: симулација припрема податке, а рендерер их конзумира у јасно дефинисаном
тренутку петље. На тај начин се избегавају нејасна преклапања одговорности која
у `C` пројектима често воде ка имплицитним зависностима и сложеним баговима.

== Основе `wgpu` модела

Библиотека `wgpu` имплементира WebGPU концепте у Rust екосистему и обезбеђује
унификован приступ над више backend-а (Vulkan, Metal, DirectX, OpenGL у
одређеним конфигурацијама). За овај рад то има директан инжењерски значај:

- апликација остаје преносива између платформи,
- графички модел је модеран и ближи савременој GPU пракси,
- API је типски и безбедносно строжи него код класичних `C` интерфејса.

Захваљујући томе, смањује се ризик од класе грешака које се у ниско-нивоским
API-јима јављају услед неправилног управљања ресурсима.

Важно је нагласити да то не значи да Rust „магично“ уклања сву сложеност GPU
програмирања. Напротив, концепти као што су pipeline стања, синхронизација
команди, распоред бафера и животни век ресурса и даље захтевају пажљив дизајн.
Разлика је у томе што компајлер и типски систем додатно помажу да број
неконзистентних стања буде мањи, па се проблеми чешће откривају раније у развоју.

== Зашто `wgpu` уместо алтернатива

Избор `wgpu` у овом пројекту условљен је потребом да решење истовремено буде:

- довољно ниско-нивоско за учење реалног рендеринг тока,
- довољно безбедно и читљиво за академску анализу,
- довољно преносиво за различита окружења.

У поређењу са алтернативама:

- OpenGL + `glium` може бити једноставнији за почетак, али мање прати модерни
  explicit модел ресурсног управљања,
- директни Vulkan/DirectX/Metal нуде максималну контролу, али уз значајно већу
  сложеност и већу вероватноћу грешака,
- виши framework-ови (нпр. готови game engine слојеви) убрзавају прототипирање,
  али прикривају критичне детаље који су важни за циљ овог рада.

Стога `wgpu` представља рационалан компромис: задржава се практична дубина,
а избегава се непотребна сложеност.

У контексту поређења са `C` и `C#`, овај компромис је посебно видљив. `C` + Vulkan
даје највећу контролу, али је број детаља који програмер мора ручно да одржава
веома висок. `C#` решења вишег нивоа обично омогућавају брже прототипирање, али
често сакривају кључне детаље GPU пута који су за овај рад методолошки важни.
Rust + `wgpu` је одабран јер дозвољава да се ти детаљи уче и анализирају, а да се
истовремено минимизује ризик од класичних memory-safety грешака у апликативном
коду.

== Пут од ECS компоненти до draw позива

Кључни кораци у сваком кадру су:

1. избор видљивих ентитета,
2. формирање инстансних података (позиција, скала, ротација, UV/текстура),
3. упис података у GPU бафере,
4. подешавање pipeline стања,
5. извршавање инстансног цртања.

Овакав приступ смањује број draw позива када више објеката дели исти pipeline и
текстурне ресурсе, што је важно за стабилан FPS при већим сценама.

== Шејдерски слој и ресурсни модел

Рендеринг подсистем користи шејдере као формалну дефиницију трансформације
геометрије и боје. Иако је конкретна логика шејдера у пројекту сведена на 2D
случај, архитектонски шаблон је скалабилан:

- вршни шејдер дефинише позиционирање у простору кадра,
- фрагментни шејдер управља читањем текстуре и бојом пиксела,
- bind групе раздвајају униформе, текстуре и самплере у јасне ресурсне блокове.

Та дисциплина у раду са ресурсима је директно усклађена са циљевима модерних
API-ја: предвидивост, експлицитност и лакша оптимизација.

== Управљање редоследом исцртавања

У 2D контексту редослед исцртавања (z-order, layer) је функционално критичан.
Рендерер зато приликом формирања batch-а води рачуна о редоследу објеката.
Концептуално, то омогућава исправно преклапање спрајтова без ослањања на сложени
3D depth pipeline.

Успостављањем јасног правила редоследа, визуелни резултат постаје стабилан и
репродуктиван, што је важно и за тестирање и за бенчмарк поређења.

Репродуктивност је у овом раду значајна не само због визуелне коректности, већ и
због валидности мерења. Ако редослед приказа варира без јасног правила,
перформансне разлике између покретања могу бити последица неочекиваног стања, а
не стварне промене у архитектури. Зато су детерминисана правила припреме batch-а
и draw редоследа важан део методологије евалуације.

== Перформансне импликације

Перформансе рендеринга зависе од више фактора:

- броја инстанци у кадру,
- учесталости ажурирања бафера,
- броја прелаза стања pipeline-а,
- трошка синхронизације између CPU и GPU стране.

У контексту овог пројекта, најважнији ефекат има број активних објеката и
квалитет batch-овања. Зато је у `rendering-benchmark` апликацији фокус стављен
на контролисано повећање броја објеката уз праћење FPS метрике.

Поред тога, релевантно је посматрати и карактер пада перформанси. Линеарни пад
FPS-а са растом оптерећења обично указује на предвидиво понашање система, док
нагли нелинеарни прекиди могу бити сигнал да је достигнут праг капацитета
конкретне компоненте (нпр. write path ка GPU баферима или учесталост смене
pipeline стања). Такав тип анализе је важан јер помера фокус са „колико FPS има“
на „зашто се FPS мења“.

== Ограничења тренутне имплементације

Иако рендеринг подсистем испуњава пројектне циљеве, уочљива су и ограничења:

- примарно је усмерен на 2D спрајт сценарио,
- не садржи напредне технике (instanced texture atlases, culling стратегије,
  мултипас ефекте),
- не обухвата дубљу аутоматску оптимизацију распореда ресурса.

Ово, међутим, не представља архитектонску препреку за даљи развој. Напротив,
постојећи дизајн је довољно чист да омогући постепено додавање сложенијих
функционалности без ломљења јавног интерфејса.
