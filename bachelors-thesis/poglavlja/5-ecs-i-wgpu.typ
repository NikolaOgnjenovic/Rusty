= Имплементација рендеринг слоја уз _wgpu_

Након дефинисања и имплементације основног _ЕКС_ језгра у оквиру _Rusty_ погона,
неопходно је омогућити визуелизацију стања света ради лакшег праћења симулација и развоја видео игара.
У овом поглављу описан је поступак имплементације рендеринг слоја који користи _wgpu_ библиотеку за хардверски убрзано исцртавање дводимензионалних елемената.

== Архитектура рендеринг слоја

Главна компонента рендеринг система је структура `Renderer2D`, која служи као омотач око _wgpu_ графичког контекста.
Она је одговорна за управљање животним циклусом графичког уређаја, креирање и ажурирање _wgpu_ ресурса (као што су бафери и текстуре) и извршавање команди за цртање.

Иницијализација рендерера је комплексан процес који захтева координацију неколико кључних _wgpu_ објеката:
- `Instance`: почетна тачка, обезбеђује приступ графичким адаптерима.
- `Adapter`: представља физички графички уређај (нпр. графичка картица).
- `Device` и `Queue`: `Device` је логички уређај који омогућава креирање ресурса, док `Queue` служи за слање команди графичком процесору.
- `Surface` и `Swapchain`: дефинишу прозорски контекст и механизам за двоструко баферовање (енг. _double buffering_).

Функција `new_async` асинхроно иницијализује ове ресурсе, прилагођавајући се доступном графичком _АПИ_-ју (_Vulkan_, _Metal_, _DirectX 12_) на домаћинском оперативном систему.
Ова апстракција је кључна за преносивост и перформансе.

== Основе _wgpu_ ресурсног модела

_wgpu_ захтева експлицитно управљање ресурсима. У оквиру _Rusty_ погона, рендеринг је заснован на неколико основних градивних блокова:

=== Вертексни и индексни бафери

Спрајтови се представљају као правоугаоници (квадови).
Дефинисани су помоћу `QUAD_VERTICES` (позиције темена и текстурне координате) и `QUAD_INDICES` (индекси за рендеровање троуглова).
Ови подаци се преносе на меморију графичког процесора путем бафера, што омогућава ефикасно исцртавање стотина спрајтова у једном позиву.

=== Рендеринг пајплајн

Рендеринг пајплајн дефинише стање графичког процесора. Он укључује:
- Шејдер модул: скуп _WGSL_ инструкција за обраду темена и фрагмената.
- `PipelineLayout`: дефинише како су ресурси (униформе, текстуре) мапирани у шејдере.
- `PrimitiveState`: конфигурација за растеризовање (нпр. начин слагања троуглова).

=== Везивне групе

Оне омогућавају шејдерима приступ подацима као што су текстуре (`TextureView`), самплери (`Sampler`) и униформни бафери (нпр. матрице камере).
Користећи везивне групе, _Rusty_ ефикасно преноси податке између процесора и графичког процесора.

== Систем ресурса и текстура

Управљање текстурама је реализовано путем `TextureId` идентификатора који служи као апстрактна референца.
`Renderer2D` одржава `HashMap<TextureId, TextureResource>` који повезује идентификаторе са стварним графичким ресурсима у меморији.

Учитавање текстура користи `image` библиотеку за декодирање података са диска у _RGBA_ формат,
након чега се подаци преносе на графички процесор помоћу `wgpu::util::DeviceExt::create_texture_with_data`.
Овај систем омогућава динамичко додавање текстура током рада погона, без потребе за поновним покретањем графичког пајплајна.

== Интеграција са _ЕКС_ компонентама

Повезивање рендеринг слоја са _ЕКС_ језгром остварено је дефинисањем специјализованих компоненти које садрже податке потребне за исцртавање:

- `Transform2D`: чува информације о позицији, ротацији и размери ентитета у простору сцене.
- `SpriteComponent`: дефинише визуелне карактеристике ентитета, укључујући идентификатор текстуре,
    изворни правоугаоник за мапирање текстуре (енг. _texture atlas_), величину приказа, слој исцртавања (енг. _z-index_) и боју (енг. _tint_).

Ове компоненте се обрађују током извршавања `RenderSystem`-а, који представља спону између података сцене и графичког уређаја.

== `RenderSystem` и петља рендеровања

`RenderSystem` је стандардни _ЕКС_ систем који се извршава унутар циклуса игре.
Његова улога је да приступи свим ентитетима који поседују и `Transform2D` и `SpriteComponent`
и споји њихове податке у облик погодан за графички процесор и проследи их рендереру.

Унутар `RenderSystem::run` методе, врши се закључавање инстанце `Renderer2D` помоћу `Arc<Mutex<Renderer2D>>`,
чиме се осигурава безбедан приступ графичким ресурсима из вишенитног окружења:

```rust
impl System for RenderSystem {
    fn run(&mut self, world: &mut World) {
        if let Ok(mut renderer) = self.renderer.lock() {
            renderer.render_world(world);
        }
    }
}
```

Метода `render_world` прикупља видљиве спрајтове, сортира их према слоју исцртавања како би се обезбедило правилно слагање слојева,
и на крају позива функцију `render_batch_with_order`.
Ова функција групише спрајтове са истим текстурама како би се смањио број _draw_ позива, што је кључна оптимизација за перформансе.

== Употреба _wgpu_ за графичко убрзање

_wgpu_ је одабран јер пружа апстракцију високог нивоа која задржава перформансе графичких _АПИ_-ја ниског нивоа.
У оквиру _Rusty_ рендерера, _wgpu_ омогућава коришћење сопствених шејдера писаних у _WGSL_ језику (енг. _WebGPU Shading Language_).

Шејдер `sprite.wgsl` извршава следеће операције:
1. Вертексни шејдер преузима податке о трансформацији (позиција, ротација, скала) из инстансних бафера и претвара их у _Clip Space_ координате, користећи матрицу пројекције камере.
2. Фрагментни шејдер узоркује (енг. _sampling_) текстуру, примењује боју и врши алфа-блендовање пиксела.

Овакав дизајн омогућава да се већина тешких операција (нпр. матричне трансформације, апроксимација боја) извршава на графичком процесору,
док _ЕКС_ језгро остаје растерећено и фокусирано на управљање логиком игре и стањем света.
