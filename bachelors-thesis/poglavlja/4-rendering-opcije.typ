= Опције за рендеринг слој у _Rusty_ погону

Да би се перформансе и понашање _ЕКС_ језгра могле испитати у условима који одговарају стварној употреби,
потребно је имплементирати и једноставан рендеринг слој.
Иако рендеринг није у фокусу овог рада, његово постојање омогућава изградњу потпуно функционалне дводимензионалне игре
која ће служити као окружење за симулацију и стрес тестирање развијеног _ЕКС_ система.
На тај начин могуће је посматрати понашање језгра у сценаријима који укључују велики број ентитета,
честе измене компоненти и континуирано извршавање система током рендеровања кадрова.

Из тог разлога потребно је одабрати библиотеку или апстракциони слој за рендеринг који ће се уклопити у архитектуру погона
и омогућити реализацију тестних сценарија без увођења непотребних ограничења.
Изабрано решење треба да задовољи следеће захтеве:

- да не намеће сопствени циклус игре,
- да омогући ефикасно приказивање великог броја спрајтова и текстура, како би се могла симулирати оптерећења карактеристична за стварне игре,
- да подржава прилагођавање рендеринг процеса ради лакшег повезивања са _ЕКС_ архитектуром,
- да буде мултиплатформско решење доступно на оперативним системима _Windows_, _macOS_ и _Linux_,
- да омогући писање апликативног слоја у безбедном _Rust_ коду.

У оквиру _Rust_ екосистема могу се издвојити три основна приступа за реализацију овог слоја:

- директно коришћење графичких _АПИ_-ја ниског нивоа (_wgpu_, _ash_, _vulkano_),
- апстракционе библиотеке засноване на постојећим _АПИ_-јима (_pixels_, _softbuffer_),
- наменске библиотеке за дводимензионалне игре (_macroquad_, _ggez_, _raylib_),

== Графичке библиотеке ниског нивоа

=== _ash_

_ash_ је _Rust_ омотач (енг. _wrapper_) око _Vulkan_ графичког _АПИ_-ја @ash.
Библиотека пружа готово директан приступ _Vulkan_ функцијама без значајних апстракција изнад самог _АПИ_-ја.

_ash_ је погодан за ситуације у којима програмер жели потпуну контролу над свим аспектима _Vulkan_ извршног окружења.
Ова карактеристика га чини моћним, али и захтевним: за елементарно цртање троугла потребно је ручно
иницијализовати инстанцу, физички и логички уређај, командне редове, пролаз за рендеровање (енг. _render pass_),
графички пајплајн (енг. _pipeline_), бафере за слике и синхронизационе примитиве.

Додатно, _ash_ нема подршку за _Windows_, _macOS_ и _Linux_ кроз јединствени апстракциони слој на нивоу библиотеке.
Мултиплатформска подршка захтева ручно руковање разликама између платформи и интеграцију са библиотеком попут _winit_ за управљање прозорима.

Главна мана _ash_-а у контексту _Rusty_ погона јесте недостатак апстракције јер сваки детаљ _Vulkan_ инфраструктуре постаје одговорност програмера.

=== _vulkano_

_vulkano_ је _Rust_ библиотека која уводи слој апстракције изнад _Vulkan_ _АПИ_-ја @vulkano.
За разлику од _ash_-а, _vulkano_ аутоматски управља делом синхронизационих и меморијских детаља,
нудећи апстракцију ресурса са јаснијим моделом власништва у духу _Rust_-а.

_vulkano_ генерише типски безбедне омотаче за _Vulkan_ структуре током компајлирања,
чиме се одређена класа грешака у употреби _Vulkan_ ресурса може открити пре извршавања програма.

Ипак, _vulkano_ остаје везан искључиво за _Vulkan_, па нема изворне подршке за _macOS_ (где је доступан
само _Metal_ преко _MoltenVK_ слоја) нити за _WebAssembly_.
Мултиплатформска компатибилност захтева додатну конфигурацију и ослањање на средства трећих лица.

Са становишта погона за дводимензионалне игре, _vulkano_ и даље уводи знатну сложеност приликом постављања иницијалне инфраструктуре.

== Апстракционе библиотеке

=== _pixels_

_pixels_ је библиотека за хардверски убрзан рендеринг пиксел-бафера (енг. _pixel buffer_) у _Rust_-у @pixels.
Основна идеја је да програмер директно пише пикселе у меморијски бафер у _RGBA_ формату,
а _pixels_ тај бафер ефикасно преноси на екран преко _wgpu_.

Употреба је намерно поједностављена: _pixels_ ствара прозор, управља површином за цртање
(енг. _surface_) и брине о размеривању (енг. _scaling_) пиксел-бафера на стварну резолуцију прозора.

```rust
let mut pixels = {
    let surface_texture = SurfaceTexture::new(WIDTH, HEIGHT, &window);
    Pixels::new(WIDTH, HEIGHT, surface_texture)?
};

let frame = pixels.frame_mut();
for pixel in frame.chunks_exact_mut(4) {
    pixel.copy_from_slice(&[0x48, 0xb2, 0xe8, 0xff]);
}
pixels.render()?;
```

_pixels_ је одличан за прототипове, демо апликације и игре у стилу ретро пиксел уметности
где се графика генерише потпуно процесорски, без употребе текстура и шејдера.

Ограничење _pixels_ библиотеке за потребе _Rusty_ погона је архитектурална природа саме библиотеке.
Она не нуди механизме за управљање текстурама, спрајтовима, беч рендерингом (енг. _batching_) нити за прилагођене шејдере.
Сваки елемент игре морао би ручно да се растеризује у пиксел-бафер, без хардверске подршке за операције попут трансформација и блендовања.

=== _softbuffer_

_softbuffer_ је библиотека за директно цртање пиксела у прозор без убрзања графичким процесором @softbuffer.
Она апстрахује разлике између платформских _АПИ_-ја (нпр. _Win32_, _X11_, _Wayland_, _CoreGraphics_)
и излаже јединствен интерфејс за писање у фрејм бафер (енг. _framebuffer_).

_softbuffer_ је намењен искључиво процесорском рендерингу и нема никакву интеграцију са графичким процесором.
За потребе _Rusty_ погона, где је убрзање графичким процесором важно чак и у дводимензионалном контексту,
_softbuffer_ није одговарајући избор.

== Наменске библиотеке за дводимензионалне игре

=== _ggez_

_ggez_ је _Rust_ библиотека инспирисана _LÖVE_ радним оквиром за _Lua_ @ggez.
Циљ библиотеке је да пружи минималан и прагматичан скуп алата за дводимензионалне игре
без уводног трошка постављања графичке инфраструктуре ниског нивоа.

_ggez_ нуди:

- цртање спрајтова и геометријских примитива,
- управљање звуком,
- улазне догађаје,
- управљање ресурсима путем система датотека.

Унутар _ggez_ библиотеке рендеринг је реализован преко _wgpu_, али ова чињеница остаје скривена
иза апстракционог слоја библиотеке.

Кључно ограничење _ggez_ библиотеке за потребе _Rusty_ погона је чврста спреженост петље игре са унутрашњим догађајима библиотеке.
Кориснички код мора да прати унапред одређени образац постављања и ажурирања (нпр. имплементација `EventHandler` _trait_-а),
чиме се смањује могућност слободне интеграције са спољним _ЕКС_ слојем.

=== _macroquad_

_macroquad_ је минималистичка библиотека за дводимензионалне игре у _Rust_-у @macroquad.
Дизајнирана је са циљем да смањи улазну баријеру за развој игара: не захтева спољну петљу ни
сложену иницијализацију, а основни приказ на екрану може се остварити у неколико линија кода.

```rust
use macroquad::prelude::*;

#[macroquad::main("Game")]
async fn main() {
    loop {
        clear_background(BLACK);
        draw_circle(200.0, 200.0, 30.0, YELLOW);
        next_frame().await;
    }
}
```

_macroquad_ подржава _Windows_, _macOS_, _Linux_ и _WebAssembly_, а нуди и функционалности
попут цртања текстура, текста, геометрије, управљања улазом и репродукције звука.

Унутрашња имплементација _macroquad_ библиотеке заснована је на _OpenGL_ (преко _miniquad_ апстракционог слоја),
а не на савременим графичким _АПИ_-јима попут _Vulkan_, _Metal_ или _DirectX 12_.

Ово је значајно ограничење за пројекте усмерене ка дугорочној компатибилности и потпуном искоришћавању савремених графичких функционалности.
_OpenGL_ је у статусу одржавања на _macOS_ (почев од верзије 10.14, _Apple_ означава _OpenGL_ као застарели _АПИ_),
а _WebGL_ (на ком _macroquad_ заснива веб подршку) нема директан еквивалент у _WebGPU_ стандарду.

Са архитектуралног становишта, _macroquad_ ствара потешкоће при интеграцији са засебним _ЕКС_ слојем
јер петља извршавања и управљање стањем постоје унутар библиотеке, умешани са логиком рендеровања.

=== _raylib-rs_

_raylib_ је популарна _C_ библиотека за развој игара, а _raylib-rs_ представља _Rust_ омотач
преко _FFI_ (енг. _Foreign Function Interface_) слоја @raylibrs.

_raylib_ пружа широк скуп функционалности: цртање дводимензионалних и тродимензионалних примитива,
управљање улазом, звуком и прозором.

Ограничење _raylib-rs_ за потребе _Rusty_ погона произилази из природе _FFI_ слоја:
корисник мора да ради са небезбедним кодом на граничном слоју преко ког се комуницира са _C_ кодом.
Ово директно противречи основној хипотези рада, која се заснива на показивању да је могуће реализовати апликативни слој погона без употребе небезбедног кода.

== Аргументација одабира _wgpu_

_wgpu_ је _Rust_ имплементација _WebGPU_ стандарда која унутрашњим слојем апстракције
подржава _Vulkan_, _Metal_, _DirectX 12_, _DirectX 11_, _OpenGL_ и _WebAssembly_ преко _WebGPU_ @wgpu.

Кључне предности _wgpu_ у односу на претходно споменуте опције за имплементацију рендеринг слоја у _Rusty_ су:

=== Мултиплатформска подршка без небезбедног кода у апликативном слоју

_wgpu_ пружа јединствен _АПИ_ за рендеринг на свим значајним платформама
без потребе да апликативни слој директно комуницира са платформски специфичним графичким _АПИ_-јем.
Унутрашњост _wgpu_ библиотеке користи небезбедне блокове тамо где је то неопходно (нпр. у интерфејсима према _Vulkan_ или _Metal_),
али та небезбедност остаје енкапсулирана унутар библиотеке и не продире у апликативни слој погона.

Ово је директно у складу са хипотезом рада: апликативни и архитектурални слој _Rusty_ погона
може бити написан у потпуности у безбедном _Rust_ коду, ослањајући се на то да _wgpu_ интерно управља небезбедним кодом.

=== Директна интеграција са _Rust_ власничким моделом

_wgpu_ _АПИ_ је пројектован у складу са _Rust_ системом власништва и позајмљивања.
Ресурси попут бафера, текстура и командних кодера управљају се преко власничких структура са јасно дефинисаним животним циклусом.
На тај начин, неисправна употреба ресурса може бити откривена у фази компајлирања уместо у тренутку извршавања програма.

=== Прилагодљивост рендеринг тока

За разлику од библиотека попут _macroquad_ и _ggez_, _wgpu_ не намеће структуру циклуса игре нити унапред одређен скуп апстракција за спрајтове или сцену.
Програмер потпуно контролише организацију рендеринг тока: структуру пролаза рендеровања, формат шејдера,
распоред везивних група (енг. _bind groups_) и редослед извршавања команди.

Ова прилагодљивост је важна за интеграцију са _ЕКС_ архитектуром,
јер рендеринг системи у _Rusty_ погону могу директно управљати ресурсима графичког процесора,
без посредних апстракција које би наметале властиту организацију сцене.

=== Активно одржавање и широка употреба у екосистему

_wgpu_ је библиотека коју активно одржава тим _Mozilla Foundation_ и _wgpu_ заједнице.
Она представља основу рендеринг слоја у _Firefox_ прегледачу (за _WebGPU_ имплементацију),
у _Bevy_ погону и у бројним другим пројектима у _Rust_ екосистему.