= Имплементација ECS-а у `rusty`

Ово поглавље описује како су кључни ECS концепти конкретно реализовани у
библиотеци `rusty`. Фокус је на структури `World`, моделу компоненти,
механизму догађаја и организацији система, уз осврт на предности и ограничења
изабраног решења.

== Ентитети као лагани идентификатори

У ECS моделу ентитет не садржи доменску логику, већ представља идентификатор над
којим се везују компоненте. Такав приступ омогућава да један ентитет по потреби
добија или губи карактеристике у току извршавања, без промене типске хијерархије.

Практична последица је да животни циклус ентитета постаје једноставан:

- креирање новог ID-а,
- додавање иницијалног скупа компоненти,
- уклањање ентитета и придружених података када више није активан.

Овакав модел је посебно погодан за игре, где се број и тип објеката често
динамички мењају током једне сцене.

== Компоненте као чисти подаци

Компоненте су у пројекту осмишљене као структуре података без пословне логике.
На пример, позиција, брзина, здравље, ознака видљивости или текстурни подаци
представљају независне компоненте. Логика која их обрађује смештена је у
системима.

Та подела има неколико важних последица:

- компоненте су једноставније за сериализацију,
- компоненте се лакше тестирају и поново користе,
- комбинацијом више компоненти добија се широк спектар понашања без наслеђивања.

У библиотеци је ово подржано генеричким API-јем, где се компоненте адресирају по
типу. Типска безбедност језика Rust ту игра кључну улогу јер умањује ризик од
грешака при приступу погрешном типу података.

Употреба Rust-а у овом слоју је посебно значајна јер компајлер намеће дисциплину
која у `C` пројектима често остаје само договор у тиму. Када систем тражи
`&mut` приступ некој компоненти, јасно је да у том делу кода не може постојати
друга активна мутабилна референца на исте податке. Такав модел умањује читаву
класу конкурентних и логичких грешака. Истовремено, у поређењу са `C#` приступом,
овде је мање ослањања на runtime проверу и више гаранција се добија већ у фази
компајлације.

== Структура `World` као центар координације

`World` је централни координациони објекат ECS језгра. Његова улога је да обједини:

- управљање ентитетима,
- приступ складишту компоненти,
- размену догађаја између система.

#figure(
```rust
pub fn create_entity(&mut self) -> Entity
pub fn destroy_entity(&mut self, entity: Entity)
pub fn add_component<T: Component>(&mut self, entity: Entity, component: T)
pub fn get_component<T: Component>(&self, entity: Entity) -> Option<&T>
pub fn get_component_mut<T: Component>(&mut self, entity: Entity) -> Option<&mut T>
```,
 caption: [Основне операције над ECS светом]
) <lst-world-core>

Из перспективе архитектуре, `World` представља #emph[једну тачку истине] о
тренутном стању симулације. Овај избор поједностављује развој и отежава појаву
дуплираних извора стања, што је честа грешка у сложенијим системима.

Цена овог избора је што API мора пажљиво да се обликује како би остао практичан
за употребу. У Rust-у то често значи да се ток података дизајнира у „фазама“:
прво читање, затим израчунавање, па упис, уместо слободног мешања приступа као у
мање строгим моделима. На први поглед ово делује као додатно оптерећење за
програмера, али се у већим изменама показује корисним јер подстиче чисто
раздвајање одговорности унутар система.

== Складиште компоненти и регистар типова

Имплементација чува компоненте по типовима преко менаџера складишта. Кључна идеја
је да сваки тип компоненте има сопствени storage, док је приступ апстрахован кроз
заједнички интерфејс.

#figure(
```rust
pub trait ComponentStorage {
    fn remove(&mut self, entity: Entity);
    fn serialize_storage(&self) -> serde_json::Value;
    fn deserialize_storage(&mut self, value: serde_json::Value);
}

pub struct ComponentManager {
    storages: HashMap<TypeId, Box<dyn ComponentStorage>>,
    names_to_typeids: HashMap<String, TypeId>,
}
```,
  caption: [Апстракција складишта компоненти у `rusty`]
) <lst-component-storage>

Предност ове архитектуре је једноставна динамичка регистрација и приступ
компонентама по типу. Недостатак је нешто већи overhead у односу на компактне
архетајп структуре, посебно код екстремно великих сцена. Ипак, за потребе овог
рада ово решење даје повољан баланс између јасноће имплементације и практичне
употребљивости.

Овде је битно истаћи и ограничење које је свесно прихваћено: циљ није био да се
одмах направи најбржа могућа ECS варијанта, већ да се покаже да и са безбедним
апстракцијама може да се добије стабилан систем. У том смислу, Rust је послужио
као филтер квалитета архитектуре: ако је неки део модела тешко изразити без
`unsafe`, то је често сигнал да интерфејс или ток података треба додатно
појаснити.

== Системи и догађаји

Системи представљају извршне јединице које читају и мењају стање у `World`-у.
Комуникација између система реализована је преко догађаја, чиме се смањује директна
спрега. Уместо да системи међусобно позивају методе, један систем емитује догађај,
а други га преузима у свом циклусу обраде.

#figure(
```rust
pub fn push_event<E: 'static + Send>(&mut self, event: E)
pub fn take_events<E: 'static + Send>(&mut self) -> Vec<E>
```,
  caption: [Основни API за рад са догађајима]
) <lst-events-api>

Овим приступом се добија:

- боља декомпозиција логике по функционалним целинама,
- једноставнија замена или проширење система,
- лакше тестирање јер системи могу да се изолују по улазним догађајима.

Догађајни модел је посебно користан у контексту Rust-а јер смањује потребу да
више система истовремено држи мутабилне референце на `World`. Уместо директног
повезивања, системи размењују намере кроз догађаје, па се обрада може
секвенцијално организовати уз јасно дефинисан ownership ток. Ово је пример како
језичка ограничења не морају бити препрека, већ могу да воде ка чистијем
архитектонском дизајну.

== Пример из `text-game` пројекта

Текстуална игра илуструје практичан ECS ток у минималном окружењу. `AttackEvent`
представља улазни подстицај, док `DamageSystem` обрађује догађај и ажурира
компоненте као што су `Health` и `Defending`.

#figure(
```rust
impl System for DamageSystem {
    fn run(&mut self, world: &mut World) {
        let attacks = world.take_events::<AttackEvent>();
        for attack in attacks {
            let mut damage = attack.damage;
            if is_defending(world, attack.target) {
                damage = (damage / 2).max(0);
            }
            if let Some(h) = world.get_component_mut::<Health>(attack.target) {
                h.hp = (h.hp - damage).max(0);
            }
        }
    }
}
```,
  caption: [ECS обрада борбеног догађаја у `text-game`]
) <lst-textgame-damage>

Овај пример демонстрира да ECS модел није условљен графичким API-јем и да се
једнако успешно примењује у системима који имају искључиво симулациони карактер.

== Поређење са алтернативним имплементацијама

У литератури и пракси постоје две доминантне имплементационе стратегије:

- #emph[sparse-set / hash-map оријентисан приступ],
- #emph[архетајп (archetype) приступ].

Решење у овом раду је ближе првој групи. Његове главне предности су:

- лакша имплементација и разумевање,
- флексибилност у раду са динамичким типовима компоненти,
- погодност за образовне и прототипске пројекте.

Главна ограничења у односу на архетајп системе су:

- слабија локалност података,
- већи трошак итерације код масовне обраде,
- мање ефикасно пакетирање за SIMD/кеш оптимизације.

Ипак, за контекст овог рада (учење архитектуре, јасна анализа и практична
демонстрација) ова ограничења су прихватљива. Најважније је да решење остаје
довољно транспарентно да се могу јасно мерити ефекти будућих оптимизација.
Другим речима, постављен је стабилан базни модел на који је могуће надограђивати
перформансне технике без потпуног редизајна кода.

== Инжењерске импликације за даљи развој

Тренутна имплементација обезбеђује стабилну основу за даљу еволуцију:

- увођење паралелног извршавања система на основу графа зависности,
- прелазак ка архетајп организацији компоненти у критичним сценаријима,
- проширење механизма догађаја приоритетним редовима и временским ознакама,
- унапређење сериализације ради снапшот/реплеј подршке.

Сви ови правци су компатибилни са постојећим API-јем ако се задржи јасна граница
између јавног интерфејса и интерне реализације.

