= Евалуациона апликација и тестирање перформанси

Ради објективне евалуације перформанси _Rusty_ погона и његовог _ЕКС_ језгра, развијена је наменска евалуациона апликација.
Ова апликација служи као визуелни стрес-тест који омогућава посматрање понашања система у условима високог оптерећења,
користећи искључиво функционалности самог _Rusty_ погона без ослањања на спољне графичке библиотеке или друга готова решења.

== Функционалности евалуационе апликације

Евалуација се заснива на симулацији великог броја ентитета (квадрата) који се независно крећу унутар прозора.
Апликација омогућава кориснику интерактивну контролу над параметрима симулације у реалном времену:
- Динамичко скалирање броја ентитета: Корисник може повећавати или смањивати број спрајтова на екрану,
    чиме се директно мења оптерећење _ЕКС_ система и број _draw_ позива.
- Контрола брзине кретања: Могуће је убрзати или успорити кретање ентитета,
    што утиче на учесталост ажурирања `Transform2D` компоненти унутар сваког кадра.
- Праћење перформанси: Апликација континуирано рачуна и приказује _ФПС_ у наслову прозора.

Евалуациона апликација показује како се систем рендеровања носи са великим бројем спрајтова,
и колико ефикасно _ЕКС_ језгро процесира ажурирања позиција пре него што се подаци проследе графичком процесору.

== Техничка имплементација евалуације

Језгро евалуационе апликације чини структура `PerfScene`, која управља стањем симулације и _ЕКС_ светом (`World`).
За сваки ентитет у евалуацији, поред основних компоненти (`Transform2D`, `SpriteComponent`),
придружена је и компонента `Velocity2D` која дефинише вектор брзине.
Логика симулације је смештена у методу `simulate`, која у сваком кадру ажурира позиције ентитета на основу њихове брзине,
времена које је протекло од претходног кадра (`dt`), и глобалног фактора убрзања.
Приликом ажурирања, примењује се и једноставна детекција колизија са ивицама прозора.
Када ентитет дотакне границу екрана, вредност његове компоненте брзине се обрне,
обезбеђујући да сви спрајтови остану унутар видљивог подручја.

Прецизно мерење перформанси реализује се коришћењем "клизећег прозора" времена.
Унутар структуре `PerfScene` прате се два бројача: `fps_timer` (акумулирано време у секундама) и `fps_frames` (бројач кадрова).
Сваких 0,25 секунди, апликација израчунава вредност _ФПС_-а према формули `fps_frames / fps_timer`.
Након овог интервала, бројачи се ресетују, чиме се обезбеђује стабилан и читљив приказ перформанси,
без наглих скокова који би били присутни код мерења појединачних кадрова.
Ова вредност се затим приказује у наслову прозора, који се ажурира тек када је то неопходно, како би се смањио трошак системских позива.

== Анализа перформанси

Евалуациона апликација је покренута у `release` моду користећи `cargo run -r`, на `Windows 11` систему са _RTX 3050 6GB Laptop GPU_ и _Intel Core i5-13420H_ процесором.

Евалуациона апликација приказује да _Rusty_ погон за видео игре у ~60 _ФПС_-a подноси 37000 ентитета различите величине који се крећу насумично по прозору.
#figure(
    image("../slike/stress_test_37000.png", width: 80%),
    caption: [Приказ евалуационе апликације са 37000 ентитета],
) <fig-benchmark>

На екстремној евалуацији са 500 хиљада ентитета, апликација тече у ~2 _ФПС_-а,
иако се користи ~25% капацитета графичке картице уређаја на ком је покренута, као и свега 10% процесора.
То имплицира да је _Rusty_ погон за видео игре могуће унапредити паралелном вишенитном обрадом података и оптимизованијим рендеринг системом.
Свакако, више десетина хиљада ентитета у дводимензионалном простору који теку у ~60 _ФПС_-а су задовољавајуће перформансе
за једнонитни систем који прати основне _ЕКС_ принципе и користи основе _wgpu_-a.

Ова мерења указују на то да је _Rusty_ стабилна библиотека која се може користити за израду мање комплексних видео игара
и стабилних симулација до пар десетина хиљада ентитета у једном тренутку.
