= Увод
<увод>

Развој савремених видео игара захтева архитектуру која истовремено омогућава
високу флексибилност, јасну поделу одговорности и предвидиво понашање у условима
раста сложености система. Традиционални приступи засновани на дубоким
хијерархијама класа често доводе до јаке спреге између доменске логике,
симулације и рендеринга, што временом отежава одржавање и проширење пројекта.

У том контексту, парадигма #emph[Entity Component System] (ECS) се показала као
веома погодан архитектонски образац за играчке погоне, посебно када је циљ
модуларност и једноставно додавање нових функционалности. Основна идеја ECS-а је
раздвајање идентитета објекта (ентитет), његовог стања (компоненте) и понашања
(системи). Оваква подела омогућава да се сложено понашање добија композицијом,
уместо наслеђивањем, чиме се редукује број нежељених зависности у коду.

Предмет овог рада је пројекат `Rusty`, који обухвата:

- библиотеку `rusty`, као језгро ECS и рендеринг функционалности,
- апликацију `text-game`, која демонстрира ECS у текстуалном домену,
- апликацију `rendering-benchmark`, намењену мерењу перформанси рендеринга при
    различитим нивоима оптерећења.

Пројекат је реализован у програмском језику Rust, који својим моделом
власништва, позајмљивања и типске безбедности подстиче писање свеобухватног и
предвидивог кода. Комбинација ECS архитектуре и Rust-а представља природан
избор за изградњу основе погона за видео игре чији код је лак за тестирање и проширивање.

За израду погона за игре изабран је Rust како би се одговорило на питање:
да ли је могуће изградити функционално ECS језгро и процесни ток рендеровања без увођења небезбедних блокова кода.
Одговор је важан јер у домену играчких погона често постоји претпоставка
да су оптимизације ниског нивоа, које захтевају небезбедне блокове кода,
нужне и за мање комплексне погоне видео игара.

== Проблем, мотивација и контекст

Полазни проблем који овај рад адресира јесте како организовати језгро једног
играчког погона тако да:

- логика игре остане одвојена од детаља приказа,
- додавање нових типова понашања не захтева измене постојеће хијерархије,
- рендеринг слој остане довољно ефикасан за већи број објеката у сцени,
- архитектура буде довољно једноставна за анализу у оквиру основних студија.

Мотивација за избор ове теме је двострука. Са једне стране, ECS је доминантан
концепт у савременим играчким погонима и представља важну инжењерску праксу. Са
друге стране, у академском контексту је користан јер омогућава јасно
раздвајање теоријског оквира и практичне имплементације, што олакшава
верификацију архитектонских одлука кроз реалне примере и мерења.

Додатна мотивација је и упоредно искуство у односу на `C` и `C#`. У `C`-у је могуће
постићи веома висок ниво контроле, али цена је повећан ризик од меморијских
грешака и већи број дефанзивних механизама које програмер мора ручно да
одржава. У `C#`-у је продуктивност висока, али је модел извршавања чешће везан за
runtime механизме који у неким сценаријима уносе мању предвидивост на нивоу
алокација и латенције. Rust је у овом раду изабран као средина која задржава
експлицитност системског програмирања, а истовремено уводи строгу компајлерску
проверу правила власништва и животног века података.

То у пракси значи да је иницијална имплементација нешто „веће цимање“ него у
`C#`-у, јер захтева више пажње у моделовању података и протоку референци. Међутим,
када се архитектура једном постави у складу са Rust ограничењима, добија се
стабилнији основни код и мања потреба за накнадним дебаговањем грешака класе
„користи после ослобађања“ или „двоструко ослобађање“, типичних за `C`.

== Циљ рада

Основни циљ рада је пројектовање, имплементација и анализа функционалног ECS
погона са пратећим примерима употребе и перформансним бенчмарком. Из овог
општег циља изведени су следећи конкретни задаци:

- формално описати ECS парадигму и упоредити је са релевантним алтернативама,
- приказати архитектуру и главне модуле `Rusty` библиотеке,
- анализирати имплементацију кључних ECS механизама у `rusty`,
- описати рендеринг подсистем и аргументовати избор библиотеке `wgpu`,
- представити дизајн и реализацију пројекта `rendering-benchmark`,
- дискутовати резултате, ограничења и правце даљег унапређења.

== Методологија

Методолошки приступ у раду комбинује:

- #emph[теоријску анализу] архитектонских образаца и технологија,
- #emph[инжењерску имплементацију] у облику библиотеке и демонстрационих
    апликација,
- #emph[експерименталну евалуацију] перформанси кроз контролисани бенчмарк.

Теоријска анализа обухвата преглед ECS концепата, типова складишта компоненти,
као и упоредну дискусију са објектно-оријентисаним приступом и варијантама
архетипске организације података. Имплементациони део обрађује структуре података,
API језгра, ток извршавања система и интеграцију рендеринг слоја. Експериментални
део користи рендеринг бенчмарк за посматрање понашања система при постепеном
увећању броја објеката и праћењу FPS метрике.

Посебан методолошки аспект рада односи се на валидирање Rust приступа без
`unsafe` кода у апликативном слоју. Та валидација није сведена на декларативну
тврдњу, већ на анализу неколико практичних критеријума:

- да ли је могуће реализовати потребан ECS API без нарушавања типске безбедности,
- да ли је рендеринг интеграција довољно експлицитна иако се не користи ручна
    манипулација сировим показивачима,
- да ли перформансе у циљном сценарију остају довољне за стабилан рад,
- да ли је код разумљив и погодан за даље проширење у оквиру студентског
    пројекта.

== Научни и практични доприноси

Допринос овог рада може се сажети у следећим тачкама:

- развијено је функционално ECS језгро прилагођено образовној и прототипској
    употреби,
- реализован је рендеринг подсистем на модерном, преносивом графичком API-ју,
- обезбеђена су два комплементарна демонстрациона сценарија (логички и
    графички),
- успостављен је репродуктиван оквир за мерење перформанси рендеринга.

Практични значај рада огледа се у томе што пројекат може послужити као полазна
основа за даљи развој играчког погона, али и као наставни пример за
архитектонско размишљање у домену интерактивних система.

== Структура рада

Рад је организован у више поглавља која прате природан ток од теоријске основе
ка практичној реализацији и евалуацији:

- у другом поглављу дат је преглед стања у области, ECS концепата, алтернатива и
    општих технолошких избора,
- у трећем поглављу описана је укупна архитектура пројекта `Rust-ECS` и улога
    појединачних подсистема,
- у четвртом поглављу анализирана је имплементација ECS језгра у библиотеци
    `rusty`,
- у петом поглављу разматран је рендеринг систем, детаљи рада са `wgpu` и
    прелаз из ECS података ка GPU представи,
- у шестом поглављу приказан је дизајн бенчмарка, методологија мерења и
    интерпретација перформансних показатеља,
- у седмом поглављу сумирани су резултати, ограничења и могући правци даљег
    развоја.

Овако дефинисана структура омогућава да се свака кључна инжењерска одлука прати
кроз целину процеса: од концептуалног разлога избора, преко имплементационог
детаља, до емпиријске провере у реалном сценарију.
