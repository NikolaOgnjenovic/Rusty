= Имплементација _ЕКС_ језгра у _Rusty_ погону

Ово поглавље детаљно описује како је _ЕКС_ архитектура имплементирана у оквиру
библиотеке _Rusty_.

За разлику од претходног поглавља, које даје теоријски и компаративни оквир
_ЕКС_ приступа, овде је фокус на конкретним структуралним одлукама у коду:
организацији ентитета, складиштењу компоненти, догађајном механизму,
извршавању система и интеграцији тих делова у централну структуру света (енг. _world_).

Код је организован модуларно и прати принцип раздвајања одговорности који је
карактеристичан за _data-oriented_ пројектовање @fabian2020dataorienteddesign.

== Преглед фајлова у _Rusty_ погону

_ЕКС_ језгро погона ослања се на следеће изворне фајлове:

- `lib.rs` - улазна тачка библиотеке, дефинише модуле и јавни _АПИ_;
- `entity.rs` - модел ентитета и управљање животним циклусом;
- `component.rs` - апстракције компоненти, складишта и менаџер компоненти;
- `event.rs` - догађајни модел и вишетипски догађајни редови;
- `system.rs` - дефиниција система и секвенцијални извршилац;
- `world.rs` - интеграциони слој који обједињује ентитете, компоненте и
    догађаје, уз могућност серијализације стања.

Ради провере исправности имплементације, сваки модул је засебно тестиран јединичним тестовима дефинисаним унутар одговарајућег изворног фајла употребом атрибута `#[cfg(test)]`.
На овај начин омогућена је изолована верификација функционалности ентитета, компоненти, догађаја, система и интеграционог слоја пре њиховог повезивања у јединствену целину.

== `lib.rs`: модулска декларација и јавни интерфејс

Фајл `lib.rs` прво излаже
унутрашње модуле (`entity`, `component`, `event`, `world`, `system`, као и `render`),
а затим реекспортује централне типове (`Entity`, `World`, `SystemExecutor` и друге)
тако да корисник библиотеке може да ради са једноставним и конзистентним _АПИ_-јем.

```rust
pub mod entity;
pub mod component;
pub mod event;
pub mod world;
pub mod system;

pub use entity::{Entity, EntityManager};
pub use component::{Component, ComponentManager, HashMapComponentStorage};
pub use event::{Event, EventManager, EventQueue};
pub use world::World;
pub use system::{System, SystemExecutor};
```

Кључна предност реекспортовања централних типова кроз коренски модул библиотеке јесте стабилност јавног _АПИ_-ја.
Кориснички код зависи од нпр. `rusty::World` или `rusty::Entity`, а не од дубоке интерне путање модула.
Практично, то смањује трошак каснијих реорганизација изворног кода,
јер унутрашње промене могу да остану транспарентне за корисника ако се спољни _АПИ_ не мења.

Алтернатива је била да се типови не реекспортују, већ да сваки клијент увози
симболе из подмодула. Тај приступ је формално исправан, али води ка дужим `use`
путањама и јачој спрези корисника са унутрашњом структуром библиотеке.

== `entity.rs`: идентитет, генерације и рециклажа ентитета

Модул `entity.rs` дефинише две централне структуре: `Entity` и `EntityManager`.

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord, Serialize, Deserialize)]
pub struct Entity {
    pub id: u32,
    pub generation: u32,
}
```

`Entity` је реализован као пар `(id, generation)`, где је `id` идентификатор, а `generation` генерација тог идентификатора.
Ово је познат образац за решавање проблема „висећих“ референци на обрисане ентитете у _ECS_ системима @lord2007evolveyourhierarchy.

`EntityManager` одржава:

- `next_id` за доделу нових идентификатора,
- `free_ids` као листу слободних идентификатора за поновну употребу,
- `generations` као вектор у коме сваки индекс директно одговара једном идентификатору,
    а вредност на тој позицији представља тренутну генерацију тог идентификатора.

```rust
#[derive(Serialize, Deserialize, Clone)]
pub struct EntityManager {
    next_id: u32,
    free_ids: Vec<u32>,
    generations: Vec<u32>,
}
```

`create` метода креира нови ентитет тако што прво покушава да рециклира слободан идентификатор из листе `free_ids`,
а ако га нема, додељује нови идентификатор и иницијализује његову генерацију са вредношћу 0.

```rust
pub fn create(&mut self) -> Entity {
    if let Some(id) = self.free_ids.pop() {
        Entity { id, generation: self.generations[id as usize] }
    } else {
        let id = self.next_id;
        self.next_id += 1;
        self.generations.push(0);
        Entity { id, generation: 0 }
    }
}
```

`destroy` метода означава ентитет као неважећи тако што проверава да ли се генерација поклапа са тренутном,
затим повећава генерацију и враћа id у free_ids ради поновне употребе.

```rust
pub fn destroy(&mut self, entity: Entity) {
    if (entity.id as usize) < self.generations.len() && self.generations[entity.id as usize] == entity.generation
    {
        self.generations[entity.id as usize] += 1;
        self.free_ids.push(entity.id);
    }
}
```

==== Зашто не забранити рециклажу идентификатора?

Потпуна забрана поновне употребе идентификатора делује безбедно, али у пракси доводи до исцрпљивања свих могућих идентификатора у дуготрајним симулацијама.
Уместо тога, рециклажа идентификатора уз употребу генерација пружа ограничен и стабилан простор идентификатора, уз задржавање безбедности референци.

=== Зашто генерације, а не само идентификатор

Када би ентитет био моделован само као `id: u32` без генерације, појавио би се проблем рециклирања идентификатора:

1. ентитет са `id = 5` се обрише,
2. исти `id = 5` се касније додели новом ентитету,
3. стари референцирани ентитет и даље постоји у системима и сада погрешно указује на нови ентитет.

Комбинација идентификатора и генерације решава овај проблем тако што сваки животни циклус истог идентификатора добија нову верзију.
Стари `(5, 0)` и нови `(5, 1)` више нису упоредиви као исти ентитет, што омогућава детерминистичку проверу валидности референци.

==== Зашто не један `u64`?

Иако би се идентификатор и генерација могли спаковати у једну 64-битну вредност, овај приступ у _ЕКС_ архитектурама има неколико недостатака.
Пре свега, он уклања експлицитну структурну разлику између идентификатора и верзије, што смањује читљивост и отежава логичко расуђивање о систему.

Поред тога, одвојено чување `id` и `generation` често омогућава боље кеш понашање у _data-oriented_ дизајну,
јер системи могу независно да обрађују идентификаторе и метаподатке о верзијама.

==== Зашто не _UUID_?

Коришћење _UUID_ (енг. _Universally Unique Identifier_) вредности обезбеђује глобалну јединственост, али за прост _ЕКС_ систем уводи значајан меморијски и перформансни трошак.
_UUID_ обично заузима 128 бита, што је четири пута више од 32-битног идентификатора, уз додатно погоршање кеш локалности приликом масовне обраде ентитета.

_UUID_ је погоднији за дистрибуиране системе и перзистентне ресурсе, али мање ефикасан у високофреквентним симулацијама са великим бројем краткоживећих објеката.


==== Инжењерски компромис

У пракси, модел заснован на пару `id` и `generation` представља компромис између перформанси, меморијске ефикасности и безбедности.
Он омогућава:

- _O(1)_ приступ ентитетима,
- ефикасно коришћење меморијског простора,
- детекцију застарелих референци без глобалне координације.

Овај приступ је широко прихваћен у савременим _ЕКС_ системима, попут _Bevy_ @bevyentityid.

== `component.rs`: типизоване компоненте преко _type erasure_ слоја

`component.rs` је најобимнији део погона и садржи комплетну инфраструктуру за складиштење компоненти различитих типова.
Сваки тип компоненте има своје засебно складиште, чиме се обезбеђује типска изолација података и избегава мешање различитих структура у меморији.

=== _Trait_ `Component`

`Component` је _marker trait_ који дефинише скуп ограничења која свака компонента мора да испуњава.
Овај _trait_ представља заједнички именитељ који омогућава да сва различита складишта компоненти буду третирана на унифициран начин у оквиру система.

Конкретно, компонента мора да:

- имплементира `Any`, чиме се омогућава идентификација типа у време извршавања,
- има `'static` животни век, што омогућава безбедно коришћење кроз механизме као што је `Any`.
- подржава серијализацију и десеријализацију путем `Serialize` и `Deserialize`,

Ова ограничења омогућавају да се типови компоненти динамички препознају током извршавања програма (коришћењем `TypeId` или `Any`),
уместо да се ослањају искључиво на статичку типску проверу у време компајлирања.

```rust
pub trait Component: Any + Serialize + for<'de> Deserialize<'de> + 'static {}

impl<T: Any + Serialize + for<'de> Deserialize<'de> + 'static> Component for T {}
```

Ограничење на `Serialize` / `Deserialize` није универзални захтев свих _ЕКС_ библиотека,
али је у _Rusty_ уведено јер `World` подржава чување и учитавање стања у _JSON_ формату преко `serde` екосистема @serde @serdejson.

Пошто сваки тип компоненте има своје засебно складиште, јавља се потреба да се та хетерогеност сакрије иза јединственог интерфејса.

=== Брисање типа складишта компоненти и динамички диспеч

У _Rust_-у, типски систем по правилу захтева да конкретни типови буду познати у време компајлирања.
Међутим, у _ЕКС_ архитектури, `ComponentManager` у једној структури управља складиштима различитих компоненти
(нпр. `Position`, `Velocity`, `Health` и сл.), при чему свака од њих има различит тип.

Да би се ово омогућило, користи се техника брисања типа (енг. _type erasure_), где се конкретни типови „скривају“
иза заједничког интерфејса `dyn ComponentStorage`. На тај начин, различита складишта могу да се чувају у истој
колекцији без познавања њиховог конкретног типа у тренутку компајлирања.

Овако добијени апстрактни слој омогућава да се сва складишта индексирају по `TypeId`, чиме се формира централни регистар свих компоненти у систему.

Конкретан тип се поново „открива“ тек када је потребно, коришћењем механизма _downcast_-а (`downcast_ref` /
`downcast_mut`). Ова операција представља контролисано враћање са динамичког типа на конкретан тип, али само
у случају када је стварни тип у складу са очекиваним.

```rust
pub trait ComponentStorage: Any {
    fn as_any(&self) -> &dyn Any;
    fn as_any_mut(&mut self) -> &mut dyn Any;
    fn remove(&mut self, entity: Entity);
}
```

У наставку се види пример враћања конкретног складишта за одређени тип `T`:

```rust
pub fn get_storage_by_type<T: Component>(&self) -> Option<&HashMapComponentStorage<T>> {
    self.storages
        .get(&TypeId::of::<T>())?
        .as_any()
        .downcast_ref::<HashMapComponentStorage<T>>()
}
```

Другим речима, јавни интерфејс остаје потпуно статички типски безбедан (нпр. `get_component::<T>`),
док се унутрашња имплементација ослања на динамички диспеч (енг. _dynamic dispatch_) како би се
омогућила хетерогена колекција складишта у једном систему.

Иако је приступ складиштима унификован, унутрашња репрезентација сваког складишта остаје специфична за конкретан тип компоненте.

=== `HashMapComponentStorage<T: Component>` и `ComponentManager`

Конкретна имплементација складишта је `HashMapComponentStorage<T>`, где је унутрашња структура `HashMap<Entity, T>`.
Зато је приступ „ентитет -> компонента“ просечно `O(1)`, уз очекиване карактеристике хеш структура.

`ComponentManager` има два речника:

- `storages: HashMap<TypeId, Box<dyn ComponentStorage>>`,
- `type_names: HashMap<String, TypeId>`.

Први речник служи за приступ складиштима по типу током извршавања кода, док други уводи стабилно текстуално име типа за серијализацију.
Ово је важно јер сам `TypeId` није погодан као интероперабилан формат за трајно чување података.

Главне операције менаџера су:

- `register<T>(name)` - региструје тип компоненте по називу и прави складиште за тај тип ако не постоји,
- `add_component` - додаје компоненту уз креирање складишта по потреби,
- `get_storage_by_type` и `get_storage_mut_by_type` - типизован приступ уз _downcast_,
- `remove_all_components` - уклања везе између свих компоненти и датог ентитета,
- `serialize_all` и `deserialize_all` - серијализација / десеријализација свих регистрованих складишта компоненти.

Овај модел имплицира да је целокупан систем организован као скуп независних речника који се централизовано управљају преко менаџера компоненти.

=== Избор `HashMap` модела за складишта компоненти и његове алтернативе

Предности `HashMap` приступа за чување свих типова складишта компоненти у `Rusty` су:

- врло једноставно додавање / брисање компоненте,
- директно адресирање преко `Entity` идентификатора,
- мала сложеност кода ради лакшег разумевања и проширивања,
- лакша серијализација речника у _JSON_.

Мане произилазе из природе речника:

- лошија кеш локалност код масовног проласка кроз компоненте,
- додатни трошак хеш рачунања,
- мање погодан модел за _SIMD_ / колонску обраду великих скупова података.

==== Архетипски приступ

Архетипски _ЕКС_ организује ентитете у „табеле“ према тачној комбинацији компоненти коју поседују.
Ако један ентитет има `(Position, Velocity)`, а други `(Position, Health)`, они припадају различитим архетипима.

Практична последица је да се компоненте чувају као колоне унутар архетипа,
па су итерације система који обрађују фиксну комбинацију компоненти често веома брзе због добре кеш локалности @fabian2020dataorienteddesign.

Цена тог приступа је „миграција“ ентитета приликом додавања или брисања компоненте:

- промена сета компоненти мења архетип ентитета,
- ентитет мора да се премести у другу табелу,
- потребно је ажурирати мапирања и индексе.

Дакле, архетипски модел је веома добар за сценарије са честим итерацијама над групама ентитета су везани за исте компоненте,
али је имплементационо сложенији од почетног `HashMap` решења.

==== _Sparse set_ организација

_Sparse set_ је компромис између једноставних мапа и строго архетипских табела. @fabian2020dataorienteddesign
Класична варијанта има:

- `dense`: компактну листу активних ентитета,
- `sparse`: речник `entity_id` -> индекс унутар `dense`,
- паралелан низ компоненти који прати `dense` редослед.

Тиме се добијају битне особине:

- брза провера постојања компоненте,
- компактна итерација по постојећим компонентама,
- релативно једноставно брисање (обично _swap-remove_ техника).

У односу на архетип, _sparse set_ је једноставнији за имплементацију и често веома добар у пракси.
У односу на `HashMap`, углавном има бољу локалност при итерацији, али захтева више пажње око индекса и конзистентности структуре.

==== _SoA_ колоне и хибридни модели

_SoA_ (_Structure of Arrays_) чува свако поље у засебној колони (нпр. све `x` координате заједно),
што додатно побољшава секвенцијално читање и _SIMD_ обраду @fabian2020dataorienteddesign.
Међутим, _SoA_ захтева пажљив дизајн _АПИ_-ја и често утиче на ергономију кода.

У индустрији су чести хибриди: архетип за често коришћене компоненте и ређи, динамични подаци у спољним структурама.
`HashMap` као полазна тачка је оправдана јер чини изборе експлицитним, а каснију миграцију ка комплекснијем складишту могућом без рушења целог _АПИ_-ја.

== `event.rs`: систем догађаја са редовима по типу

Модул `event.rs` уводи механизам асинхроне комуникације између система путем догађаја.

`Event` је _marker trait_ (`Any + 'static`), док `EventQueue<E>` представља _FIFO_ (енг. _first in, first out_) ред конкретног типа догађаја заснован на `VecDeque<E>`.

```rust
pub struct EventQueue<E: Event> {
    events: VecDeque<E>,
}

pub fn push<E: Event>(&mut self, event: E) {
    self.register::<E>();
    if let Some(queue) = self.get_queue_mut::<E>() {
        queue.push(event);
    }
}
```

За складиштење више типова редова у једном менаџеру користе се _trait_ `EventQueueTrait` и речник `HashMap<TypeId, Box<dyn EventQueueTrait>>` унутар `EventManager` структуре.
Као код компоненти, гарантује се статичка типска безбедност на _АПИ_ слоју и динамички _type erasure_ унутар менаџера.

== `system.rs`: системи и извршни редослед

`system.rs` дефинише _trait_ `System` са методом `run(&mut self, world: &mut World)`
и извршилац `SystemExecutor` који чува `Vec<Box<dyn System>>`.

```rust
pub trait System {
    fn run(&mut self, world: &mut World);
}

pub struct SystemExecutor {
    systems: Vec<Box<dyn System>>,
}

pub fn run(&mut self, world: &mut World) {
    for system in &mut self.systems {
        system.run(world);
    }
}
```

Извршавање је секвенцијално, редом којим су системи додати у вектор система, чиме се добија једноставан и детерминистички модел извршавања.
Тестови у модулу експлицитно показују да редослед утиче на резултат (нпр. увећавање пре удвостручавања вредности није исто што и удвостручавање пре увећавања вредности).

У литератури и индустријским _ЕКС_ системима често је и паралелно извршавање, где се системи извршавају конкурентно ако нема конфликтног приступа подацима.
Такав модел може да повећа искоришћење вишејезгарних процесора, али захтева значајно сложеније праћење зависности писања и брисања података и често сложенији _АПИ_.

== `world.rs`: интеграциони слој _ЕКС_ језгра погона

`World` је централна фасада језгра и садржи сва три менаџера:

- `EntityManager`,
- `ComponentManager`,
- `EventManager`.

`World` игра излаже компактни _АПИ_ и од корисника `Rusty` библиотеке сакрива комплексност унутрашњих подсистема.

```rust
pub fn destroy_entity(&mut self, entity: Entity) {
    self.components.remove_all_components(entity);
    self.entities.destroy(entity);
}

pub fn take_events<E: Event>(&mut self) -> Vec<E> {
    let mut events = Vec::new();
    if let Some(queue) = self.events.get_queue_mut::<E>() {
        while let Some(event) = queue.pop() {
            events.push(event);
        }
    }
    events
}
```

=== Животни циклус ентитета у `World`-у

Метода `destroy_entity` прво позива `remove_all_components`, па тек онда `entities.destroy(entity)`.
Овај редослед је важан јер спречава да у складиштима остану сирочад (енг. _orphan_) компоненте за ентитет који више не постоји.
У комбинацији са генерацијским моделом из `entity.rs`, добија се конзистентан механизам брисања и поновне употребе идентификатора без логичке колизије старих и нових ентитета.

=== Серијализација и десеријализација света

`World` дефинише интерну структуру `WorldSaveData` са кључним пољима:

- стање менаџера ентитета,
- серијализована складишта компоненти.

Снимање (`save`) и учитавање (`load`) реализовани су у JSON формату преко `serde_json` @serdejson.

Након учитавања се позива `events.clear()`, чиме се избегава да се стари догађаји из претходног извршавања пренесу у ново учитано стање.
Овиме стање света остаје поновљиво, а догађаји задржавају улогу транзијентног механизма комуникације, уместо да постану део трајног стања света.

== Пример примене: текстуална борбена игра

Да би се видело како описана архитектура функционише у стварној употреби, развијена је текстуална игра
у којој се `Rusty` _ЕКС_ користи за једноставну потезну борбу играча против више непријатеља:

- ентитети представљају учеснике у игри (играч и непријатељи),
- компоненте носе податке (`Name`, `Health`, `Damage`, `Defending`, маркери `Player` и `Enemy`),
- догађаји (`AttackEvent`) представљају намеру акције,
- систем (`DamageSystem`) претвара догађаје у конкретну промену стања,
- `SystemExecutor` контролише редослед извршавања,
- `World` служи као фасадни _АПИ_ кроз који се све оркестрира.

Следећи исечак кода показује почетну конфигурацију света и регистрацију система:

```rust
let mut world = World::new();

let player = world.create_entity();
world.add_component(player, Name("Hero".to_string()));
world.add_component(player, Player);
world.add_component(player, Health { hp: 45, max: 45 });
world.add_component(player, Damage { value: 7 });
world.add_component(player, Defending(false));

let mut executor = SystemExecutor::new();
executor.add_system(DamageSystem);
```

Овде се јасно види практична улога `World` _АПИ_-ја: `create_entity` враћа ентитет,
док се `add_component` позива више пута да би се формирао комплетан „профил“ тог ентитета.
Другим речима, понашање није везано за класну хијерархију, већ настаје композицијом компоненти.

Ток једног потеза реализован је кроз догађај и накнадно извршавање система:

```rust
world.push_event(AttackEvent {
    attacker: player,
    target: enemy,
    damage: dmg,
});

executor.run(&mut world);
```

Позив `push_event` смешта напад у ред догађаја, а тек у `executor.run` систем `DamageSystem` преузима све `AttackEvent` догађаје преко `take_events`
и примењује последице на `Health` циља напада (играча).
Тиме се раздваја опис самог догађаја од промене стања над циљем, у овом случају `Health` компонентом.
Догађај само бележи да је дошло до напада, док се конкретна измена животних поена играчаа извршава накнадно у оквиру система који обрађују тај догађај.

Унутар система се користе `get_component` и `get_component_mut`: први за читање тренутног стања (име, улога, вредност напада),
а други за безбедно ажурирање променљивих података (смањење `Health`).
Тако библиотека задржава јасну границу између мутабилног и имутабилног приступа подацима у оквиру једног кадра извршавања.

Компонента `Name` је минимална _tuple struct_ дефиниција (`struct Name(String);`) која служи за приказ у корисничком интерфејсу, без утицаја на саму механику борбе.
Насупрот томе, `Player` је маркер-компонента без поља (`struct Player;`). Она је сигнал системима да је реч о ентитету који представља играча.
Та разлика је јасна и у `DamageSystem`: `world.get_component::<Player>(attack.attacker).is_some()` одређује да ли се исписује порука у другом лицу („You strike...“) или у трећем („X hits you...").

И непријатељи се уводе композиционо, кроз исти _АПИ_, без посебне класе.
Уместо класе _Enemy_, у коду постоји скуп параметара (`enemy_attributes`) из ког се у петљи креира више ентитета:

```rust
let enemy_attributes = vec![
    ("Goblin", 12, 3, vec!["Slash", "Bite"]),
    ("Orc", 18, 5, vec!["Heavy Swing", "Headbutt"]),
    ("Necromancer", 22, 6, vec!["Shadow Bolt", "Bone Spike"]),
];

for (name, hp, dmg, _attacks) in &enemy_attributes {
    let e = world.create_entity();
    world.add_component(e, Name(name.to_string()));
    world.add_component(e, Enemy);
    world.add_component(e, Health { hp: *hp, max: *hp });
    world.add_component(e, Damage { value: *dmg });
    enemy_entities.push(e);
}
```

Овај део добро илуструје основну _ЕКС_ предност: нов тип противника не захтева нову хијерархију типова, већ нову комбинацију компоненти и параметара.
Компонента `Enemy` овде, као и `Player`, има улогу маркера који омогућава филтрирање и другачије понашање у логици игре.

Са становишта корисника библиотеке, логика игре се дефинише у једној `loop` петљи у функцији `main`,
у оквиру које се ручно описује редослед интеракција (акција играча, акција непријатеља и покретање система).
Са становишта _Rusty_ погона, свака фаза ове петље само активира одговарајуће механизме обраде догађаја и система.

Поједностављено, ток изгледа овако:

```rust
loop {
    // 1) услови завршетка
    // 2) читање акције играча: attack / defend / quit
    // 3) push_event(...) за напад и executor.run(&mut world)
    // 4) ако је непријатељ жив, он шаље AttackEvent ка играчу
    // 5) system_executor.run(&mut world) за непријатељски потез
}
```

Са становишта архитектуре, кључно је да циклус игре не садржи логику умањења _HP_-а, већ само оркестрацију догађаја и позив система.
Због тога је ток игре лако проширив: увођење нове акције, статус-ефекта или _AI_ правила
углавном се своди на додавање нових догађаја и / или система који их обрађују, док основна структура циклуса игре и модел оркестрације остају непромењени.
