= Стање у области

Ово поглавље даје преглед концепата и практичне имплементације у пројекту
`Rust-ECS`, који садржи библиотеку `rusty`, демонстрациону текстуалну
игру `text-game` и пројекат за мерење перформанси рендеринга
`rendering-benchmark`.

== ECS парадигма

ECS (_Entity Component System_) организује логичке целине у три слоја:

- *ентитети* су идентификатори без уграђеног понашања,
- *компоненте* су чисти подаци,
- *системи* су логика која обрађује скупове компоненти.

Ова подела смањује повезаност између делова кода и омогућава постепено ширење
функционалности без дубоког наслеђивања.

=== Како је ECS имплементиран у пројекту

У пројекту је централна структура `World`, која управља ентитетима,
компонентама и догађајима. У наставку је поједностављен исечак интерфејса:

#figure(
```rust
pub fn create_entity(&mut self) -> Entity
pub fn destroy_entity(&mut self, entity: Entity)
pub fn add_component<T: Component>(&mut self, entity: Entity, component: T)
pub fn get_component<T: Component>(&self, entity: Entity) -> Option<&T>
pub fn get_component_mut<T: Component>(&mut self, entity: Entity) -> Option<&mut T>
pub fn push_event<E: 'static + Send>(&mut self, event: E)
pub fn take_events<E: 'static + Send>(&mut self) -> Vec<E>
```,
  caption: [Кључни API методи структуре `World`]
) <lst-world-api>

Складиште компоненти је имплементирано кроз `ComponentManager` и
`HashMapComponentStorage<T>`, где се компоненте чувају по типу. Такво решење је
једноставно за разумевање и одржавање, што је одговарајуће за академски пројекат
који ставља нагласак на читљивост архитектуре.

#figure(
```rust
pub trait ComponentStorage {
    fn remove(&mut self, entity: Entity);
    fn serialize_storage(&self) -> serde_json::Value;
    fn deserialize_storage(&mut self, value: serde_json::Value);
}

pub struct ComponentManager {
    storages: HashMap<TypeId, Box<dyn ComponentStorage>>,
    names_to_typeids: HashMap<String, TypeId>,
}
```,
  caption: [Апстракција складишта компоненти и менаџер типова]
) <lst-storage>

== Алтернативе ECS приступу

Најчешће алтернативе су:

- класични _object-oriented_ приступ са дубљом хијерархијом наслеђивања,
- приступ заснован на "великим" објектима који у једној структури носе и
  податке и понашање,
- аркетајп ECS складиште (оптимизовано за масовну обраду података).

У овом раду изабран је ECS јер:

- природно раздваја доменску логику од података,
- омогућава лако додавање нових компоненти и система,
- поједностављује тестирање,
- добро се уклапа у Rust модел власништва и позајмљивања.

== Разлози избора програмског језика Rust

Избор програмског језика у овом пројекту није био секундарна техничка одлука,
већ део истраживачког циља. Централно питање било је да ли је могуће развити
функционалан ECS погон и рендеринг ток тако да сопствени апликативни код остане
без `unsafe` сегмената, уз прихватљив ниво сложености и перформанси.

У том контексту, Rust је изабран из четири кључна разлога:

- пружа memory-safety гаранције у време компилације,
- задржава експлицитан системски модел рада са ресурсима,
- има јаку подршку за data-oriented дизајн и генеричке апстракције,
- поседује зрел ecosystem за графику (`wgpu`) и алате за профилисање/тестирање.

=== Поређење са `C` приступом

У односу на `C`, Rust уводи строжа правила о ownership-у и животном веку података.
То у почетној фази развоја повећава количину архитектонског планирања, али
истовремено смањује ризик од класа грешака које су честе у ручном управљању
меморијом:

- коришћење ослобођене меморије,
- двоструко ослобађање,
- невалидне алијасне мутабилне референце,
- тешко репродуктивне runtime грешке због непредвидивог стања.

За пројекат овог типа то има директну корист: време се мање троши на „лов“
нисконивоских меморијских проблема, а више на дизајн ECS модула и анализу
перформанси. Другим речима, Rust помера сложеност са дебаговања ка ранијем,
структурнијем обликовању кода.

=== Поређење са `C#` приступом

У односу на `C#`, Rust обично захтева више почетне дисциплине, нарочито при
дефинисању интерфејса који управљају заједничким стањем. `C#` често омогућава
бржу почетну имплементацију, делом захваљујући runtime карактеристикама и
високом нивоу апстракције. Међутим, у сценаријима где су детерминизам и
предвидивост алокација важни, Rust даје већу контролу и јаснији увид у трошкове.

У практичном смислу, може се рећи да је у Rust-у „веће цимање“ на почетку,
али се тај трошак често враћа касније кроз стабилнији код и мање регресија.
Посебно у ECS моделу, где више система приступа заједничким структурама,
строга правила референци често воде ка чистијој подели одговорности.

=== Да ли је могуће без `unsafe` у пракси

Овај рад показује да је за образовни ECS погон и 2D рендеринг сценарио
могуће одржати апликативни слој без `unsafe` кода. То не значи да укупна
софтверска стек не садржи `unsafe` (библиотеке нижег нивоа га могу користити),
већ да је доменска логика и архитектонски слој пројекта могуће одржати у
безбедним границама Rust-а.

Овај резултат је важан јер демонстрира реалну применљивост Rust-а за пројекте
средњег обима: добија се повољан однос између безбедности, контроле и
инжењерске продуктивности, без неопходности да студентски пројекат улази у
комплексне и ризичне ниско-нивоске оптимизације већ у раној фази.

Истовремено, треба нагласити да тренутна имплементација користи
`HashMap`-центрирано складиште ради једноставности, док би аркетајп приступ
вероватно дао боље перформансе код великих сцена.

== Демонстрација ECS употребе: `text-game`

Пројекат `text-game` показује како се ECS користи и без графичког подсистема.
Компоненте (`Health`, `Damage`, `Defending`) описују стање, док
`DamageSystem` обрађује `AttackEvent` догађаје.

#figure(
```rust
impl System for DamageSystem {
    fn run(&mut self, world: &mut World) {
        let attacks = world.take_events::<AttackEvent>();
        for attack in attacks {
            let mut damage = attack.damage;
            if is_defending(world, attack.target) {
                damage = (damage / 2).max(0);
            }
            if let Some(h) = world.get_component_mut::<Health>(attack.target) {
                h.hp = (h.hp - damage).max(0);
            }
        }
    }
}
```,
  caption: [Обрада догађаја у систему борбе]
) <lst-text-game-system>

Овај пример је важан јер потврђује да ECS није везан за графику: исти модел
подаци–системи–догађаји ради и у терминалном окружењу.

== Рендеринг систем у `rusty`

Рендеринг слој је имплементиран око структуре `Renderer2D`, која:

- иницијализује `wgpu` инстанцу, адаптер и уређај,
- конфигурише површину приказа,
- учитава текстуре,
- прикупља видљиве спрајтове из `World` и формира _batch_.

#figure(
```rust
pub fn render_world(&mut self, world: &World) -> Result<(), RenderError> {
    let batch = collect_visible_sprites(world);
    if batch.is_empty() {
        return self.render_batch(&[]);
    }
    let mut gpu_instances = Vec::with_capacity(batch.len());
    // мапирање ECS компоненти у GPU инстанце
    self.render_batch_with_order(&gpu_instances, &batch)
}
```,
  caption: [Прелаз из ECS света у инстансни рендеринг]
) <lst-render-world>

=== Зашто је изабран `wgpu`

`wgpu` је изабран зато што нуди:

- безбедан, Rust-оријентисан API,
- преносивост на више графичких _backend_-а,
- добру основу за даљи развој 2D/3D подсистема,
- коректан однос између ниско-нивоскe контроле и продуктивности.

Алтернативе су:

- `glium`/OpenGL приступ (једноставнији почетак, али ограничења модерних токова),
- директна употреба Vulkan/DirectX/Metal API-ја (већа контрола, знатно виша
  сложеност),
- _game framework_ решења вишег нивоа (брже прототипирање уз мању контролу).

Избор `wgpu` у контексту овог рада представља компромис између преносивости,
модерног GPU модела и прихватљиве сложености имплементације.

== Рендеринг бенчмарк: `rendering-benchmark`

Пројекат `rendering-benchmark` служи за емпиријску проверу перформанси ECS +
рендеринг слоја. Сцена садржи велики број објеката (до `50_000`), а на сваком
кадру се извршава симулација кретања и исцртавање.

Кључне карактеристике бенчмарка су:

- динамичка промена броја објеката,
- контрола брзине и величине објеката,
- периодично израчунавање FPS-а,
- ажурирање наслова прозора ради визуелне повратне информације.

#figure(
```rust
if self.fps_timer >= 0.25 {
    self.fps = self.fps_frames as f32 / self.fps_timer;
    self.fps_timer = 0.0;
    self.fps_frames = 0;
    self.title_dirty = true;
}
```,
  caption: [Израчунавање FPS метрике у интервалима]
) <lst-fps>

Извођење бенчмарка је конципирано тако да корисник може интерактивно да мења
оптерећење (број објеката) и одмах посматра утицај на FPS. На тај начин се добија
практична веза између архитектонских одлука у ECS/рендеринг слоју и стварног
понашања током извршавања.

== Ограничења и правци унапређења

Иако решење испуњава циљеве рада, уочљива су и ограничења:

- `HashMap` складиште није оптимално за највећа оптерећења,
- систем извршавања је секвенцијалан,
- бенчмарк је фокусиран на један сценарио 2D спрајтова.

Логичан наставак рада је увођење аркетајп организације података,
паралелизација система и проширење скупа бенчмарк сценарија.

== Импликације за даљи развој и истраживање

У даљем развоју пројекта пожељно је раздвојити два паралелна правца:

1. #emph[архитектонско проширење] — увођење архетајп складишта, scheduler-а за
   паралелно извршавање и напредније оркестрације система,
2. #emph[методолошко проширење] — формалнија евалуација са већим бројем профила
   сцена, детаљнијим метрикама и аутоматизацијом експеримената.

Ова подела је значајна јер спречава да перформансне измене наруше јасноћу
основне архитектуре. Посебно у академском контексту, важно је да сваки корак
унапређења буде праћен мерљивим ефектом и документованом мотивацијом.

Из угла избора језика, наставак истраживања би требало да укључи и поређење
варијанти имплементације са и без агресивнијих оптимизација, како би се
квантитативно проценила граница између „безбедне архитектурне чистоће“ и
„максималних перформанси“. Таква анализа би дала још јаснији одговор на питање
када је Rust без `unsafe` довољан, а када постаје оправдано уводити сложеније
нисконивоске технике.
